import React from 'react';
import ReactDOM from 'react-dom';
import MyComponent from './MyComponent';

const documentElement = document.getElementById('app');

ReactDOM.render(<MyComponent />, documentElement);
